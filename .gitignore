# NVIDIA Isaac ROS packages (cloned from NGC/GitHub, not our code)
src/isaac_ros_common/
src/isaac_ros_visual_slam/

# TensorRT engines and large model assets
isaac_ros_assets/
*.plan
*.engine

# Large PyTorch checkpoints (not needed for deployment)
*.pth

# ONNX models (excluded by default, force-add specific ones)
*.onnx

# YOLOv10 weights
yolov10m.pt

# Python cache
__pycache__/
*.pyc
*.pyo

# ROS build artifacts
build/
install/
log/

# Container workspace build artifacts (built from source inside dev container)
lidar_ws/
realsense_ws/

# OS
.DS_Store
*.swp
*~
